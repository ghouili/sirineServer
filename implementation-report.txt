SlotyCare Backend Report (Steps 1-12)
====================================

Architecture Overview
- Stack: Node.js + Express + TypeScript + Prisma (MySQL)
- Multi-tenancy: multi-DB + shared schema; tenant-scoped tables always filtered by tenant_id
- Auth: JWT with role claims; tenant routes require active subscription
- Validation: Zod; consistent JSON response shape

Environment Variables
- DATABASE_URL
- GLOBAL_DATABASE_URL
- TENANT_DATABASE_URL
- TENANT_DB_ADMIN_URL
- TENANT_DB_HOST
- TENANT_DB_USER
- TENANT_DB_PASSWORD
- JWT_SECRET
- JWT_EXPIRES_IN
- PORT
- CORS_ORIGIN
- Gmail OAuth2 variables present for later steps

Models (Prisma Schema)
Global / Commercial
- super_admins: id, nom, email (unique), password_hash, created_at
- tenants: id, nom, slug (unique), logo_url?, status, created_at, updated_at
- licenses: id, nom, type, billing_period, duration_months, description?, prix_mensuel, features, is_active, created_at
- promotions: id, license_id, discount_rate, is_active, starts_at?, ends_at?, created_at
- subscriptions: id, tenant_id, license_id, status, billing_period, start_date, end_date,
  reference?, siret?, cabinet_nom?, cabinet_adresse?, cabinet_adresse_complete?, specialite?,
  owner_nom?, owner_prenom?, created_at, updated_at

Tenant-scoped
- users: id, tenant_id, email, password_hash, nom, prenom, role, is_active, last_login?, created_at
- patients: id, tenant_id, nom, prenom, telephone?, tags?, notes_internes?, created_at
- services: id, tenant_id, nom, duration_minutes, prix
- service_practitioners: service_id, user_id (composite PK)
- service_rules: id, tenant_id, service_id (unique), min_notice_hours, cancel_notice_hours, buffer_minutes
- working_hours: id, tenant_id, praticien_id, weekday, start_time, end_time, is_active
- breaks: id, tenant_id, praticien_id, weekday, start_time, end_time
- time_off: id, tenant_id, praticien_id, start_at, end_at, reason?
- holidays: id, tenant_id, date, label?
- appointments: id, tenant_id, patient_id, praticien_id, service_id, date_heure_debut, date_heure_fin,
  status, no_show_probability_score?, ia_recommandation?
- waitlist_entries: id, tenant_id, service_id, praticien_id?, patient_name, patient_phone?, patient_email?,
  preferred_from?, preferred_to?, status, created_at
- notifications: id, tenant_id, appointment_id?, type, channel, to_email, subject, payload, schedule_at?,
  status, tries, sent_at?
- documents: id, tenant_id, patient_id, file_path, type_document, visibility_level, uploaded_by, created_at
- document_access_logs: id, tenant_id, document_id, user_id?, patient_id?, action, ip_address?, created_at
- audit_logs: id, tenant_id, user_id, action, details?, ip_address?, created_at

Key Methods / Services
- Tenant resolution: X-Tenant-Slug -> req.tenant; blocks missing/unknown/suspended tenants
- Subscription gating: requires active subscription (status=active and now between start/end)
- Auth: JWT-based; roles: super_admin, admin, praticien, assistant
- Availability engine: working hours - breaks - time off - holidays - overlapping appointments
- Appointment create: transaction + overlap check + tenant scoping
- Waitlist: open entry on create; when appointment canceled -> queued notification + status=notified
- Documents: upload to /uploads/{tenant_slug}/{patient_id}; view/download logs access

Endpoints (Created So Far)
Health
- GET /health

Auth
- POST /auth/superadmin/login
- POST /auth/login

Tenant test
- GET /tenants/me

Superadmin
- GET /licenses
- POST /licenses
- GET /licenses/:id
- PATCH /licenses/:id
- DELETE /licenses/:id
- GET /promotions
- POST /promotions
- GET /promotions/:id
- PATCH /promotions/:id
- DELETE /promotions/:id
- GET /tenants
- POST /tenants
- GET /tenants/:id
- PATCH /tenants/:id
- DELETE /tenants/:id
- GET /subscriptions
- POST /subscriptions
- GET /subscriptions/:id
- PATCH /subscriptions/:id
- PATCH /subscriptions/:id/status
- DELETE /subscriptions/:id
- GET /kpi

Tenant Admin (role=admin)
- Users: GET /users, POST /users, GET /users/:id, PATCH /users/:id,
  PATCH /users/:id/reset-password, DELETE /users/:id
- Patients: GET /patients, POST /patients, GET /patients/:id, PATCH /patients/:id, DELETE /patients/:id
- Services: GET /services, POST /services, GET /services/:id, PATCH /services/:id, DELETE /services/:id
- Service practitioners: POST /services/:id/practitioners, DELETE /services/:id/practitioners/:userId
- Service rules: GET /service_rules, POST /service_rules, GET /service_rules/:id,
  PATCH /service_rules/:id, DELETE /service_rules/:id
- Scheduling:
  GET/POST/PATCH/DELETE /working_hours
  GET/POST/PATCH/DELETE /breaks
  GET/POST/PATCH/DELETE /time_off
  GET/POST/PATCH/DELETE /holidays

Availability
- GET /availability/slots

Appointments
- GET /appointments
- POST /appointments
- GET /appointments/:id
- PATCH /appointments/:id
- DELETE /appointments/:id

Waitlist
- GET /waitlist
- POST /waitlist
- PATCH /waitlist/:id

Documents (tenant scoped)
- GET /documents
- POST /documents
- GET /documents/:id
- GET /documents/:id/download

Public
- GET /public/documents/:id?tenant_slug=...&patient_id=...

------------------------------------------------------------
Multi-DB Provisioning (Current)
------------------------------------------------------------
Flow
- Superadmin creates a tenant via POST /tenants.
- The API automatically creates a dedicated database, applies the shared schema,
  and registers it in tenant_databases.
- If provisioning fails, the tenant is rolled back and the database is dropped.

Manual Provisioning
- POST /tenants/:id/provision-db remains available for one-off or recovery cases.
